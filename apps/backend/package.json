{
    "name": "@syncio/backend",
    "version": "1.0.0",
    "$schema": "https://json.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "description": "Syncio Assessment Backend - Laravel API",
    "scripts": {
        "build": "vite build",
        "dev": "php artisan serve --host=0.0.0.0 --port=8000",
        "test": "php artisan test",
        "lint": "vendor/bin/pint",
        "type-check": "composer validate"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.8.2",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^7.0.4"
    },
    "engines": {
        "php": ">=8.4"
    }
}