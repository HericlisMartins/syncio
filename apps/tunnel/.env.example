# Tunnel Configuration
TUNNEL_NAME=HTTP Tunnel
PORT=3000

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Target URLs
API_TARGET=http://localhost:8000
BACKEND_TARGET=http://localhost:8000

# Live Tunnel Configuration (for public access)
LIVE_TUNNEL_ENABLED=true
LIVE_TUNNEL_SUBDOMAIN=my-app
LIVE_TUNNEL_HOST=
LIVE_TUNNEL_LOCAL_HOST=
LIVE_TUNNEL_SHOW_QR=true
